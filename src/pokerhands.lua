-- SMODS.PokerHand:take_ownership (
--     'Flush Five', 
--     {
--         order = 1,

--         -- chips = 160,
--         -- mult = 16,

--         mult = 16,
--         chips = 200,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Flush House', 
--     {
--         order = 2,

--         -- chips = 140,
--         -- mult = 14,

--         mult = 14,
--         chips = 160,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Five of a Kind', 
--     {
--         order = 3,

--         -- chips = 120,
--         -- mult = 12,

--         mult = 12,
--         chips = 140,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Straight Flush', 
--     {
--         order = 4,

--         -- chips = 100,
--         -- mult = 8,

--         mult = 10,
--         chips = 120,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Four of a Kind', 
--     {
--         order = 5,

--         -- chips = 60,
--         -- mult = 7,

--         mult = 8,
--         chips = 80,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Full House', 
--     {
--         order = 6,

--         -- chips = 40,
--         -- mult = 4,

        
--         mult = 5,
--         chips = 60,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Flush', 
--     {
--         order = 7,
--         mult = 4,

--         -- chips = 35,
--         -- mult = 4,

--         chips = 45,
--     }
-- )

-- SMODS.PokerHand:take_ownership (
--     'Straight', 
--     {
--         order = 8,

--         -- chips = 30,
--         -- mult = 4,


--         mult = 4,
--         chips = 40,
--     }
-- )
-- -- SMODS.PokerHand:take_ownership (
-- --     'Two Pair', 
-- --     {
-- --         order = 10,
-- --         mult = 4,
-- --         chips = 30,
-- --     }
-- -- )

-- -- SMODS.PokerHand:take_ownership (
-- --     'Pair', 
-- --     {
-- --         order = 11,
-- --         mult = 2,
-- --         chips = 20,
-- --     }
-- -- )

-- -- SMODS.PokerHand:take_ownership (
-- --     'High Card', 
-- --     {
-- --         order = 12,
-- --         mult = 1,
-- --         chips = 10,
-- --     }
-- -- )